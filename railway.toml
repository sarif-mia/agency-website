[build]
  builder = "NIXPACKS"

[deploy]
  startCommand = "cd backend && python manage.py migrate && gunicorn agency_backend.wsgi:application"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10

[env]
  PORT = "8000"