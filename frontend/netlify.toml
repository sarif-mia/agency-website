[build]
  command = "npm run build"
  publish = "dist"
  base = "frontend/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  directory = "functions"

[dev]
  command = "npm run dev"
  port = 8888
  targetPort = 5173